#Required packages for building binaries
Debian: apt install libc-dev make g++ spawn-fcgi nginx libcrypto++-dev automake libtool psmisc pipexec libjsoncpp-dev libunwind-dev libtool automake bc coreutils unzip
Alpine: apk add autoconf automake libtool crypto++-dev cmake

openjdk-11-jdk-headless

#TRACEABILITY (packages-assets to be serviced by it)
=======================================#============

#coreutils
#  bin/us-wallet-ctl depends on nohup

#bc
#  bin/us-wallet-ctl depends on bc

#libunwind-dev
#  gov debug mode

#libtool
#  install_libsecp256k1

#automake
#  install_libsecp256k1

#libcrypto++-dev
#  gov/crypto

#nginx
#  

#spawn-fcgi
#  wallet. (with FCGI compilation flag)

#pipexec
#  etc/init.d/*

#psmisc
#   provides killall
#   etc/init.d/*
#   bin/us-wallet-ctl

#unzip
#   cash/android/make

cmake 
    bin/install_jsoncpp