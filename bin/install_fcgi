#!/bin/sh

dir=`mktemp -d`

cd $dir
git clone https://github.com/eddic/fastcgipp.git fastcgi++
mkdir build
cd build
apt install cmake
cmake -DCMAKE_BUILD_TYPE=RELEASE ../fastcgi++
make
make install
echo $dir
exit
rm $dir -r
