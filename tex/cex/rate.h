#ifndef CUREX_CEX_RATE_H
#define CUREX_CEX_RATE_H
/*
#include "cex.h"
#include <utility>
#include <cmath>
#include <iostream>

namespace curex { namespace cex {

class rate {
public:
	rate();
	rate(const efp& v);
	rate(const rate& other);

	rate& operator = (const rate& other);
	rate& operator = (rate&& other);

	static bool make_valid(efp& v);
	static bool is_valid(const efp& v);


	efp value{1};


	bool operator !=(rate const& t2) const;

	bool operator <(const rate& other) const;
	

};




}}
*/
#endif

